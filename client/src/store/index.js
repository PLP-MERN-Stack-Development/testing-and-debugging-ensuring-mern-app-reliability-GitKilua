// Re-export the main store
export { default } from './store';

// Re-export slices for convenience
export { default as bugsSlice } from './bugSlice';

// If you add more slices (e.g., for auth), export them here too
// export { default as authSlice } from './authSlice';
